import React from 'react';

import { Grid, Typography } from '@material-ui/core';

export default function CreateProductForm() {
  return (
    <Grid container>
      <Grid item xs={4}>
        <Typography variant="subtitle1">testsetestsetest</Typography>
      </Grid>
      <Grid item xs={8}>
        <Typography variant="body1">bobobobobob</Typography>
      </Grid>
    </Grid>
  )
}